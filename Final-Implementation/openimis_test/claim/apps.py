from django.apps import AppConfig

class ClaimConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "openimis_test.claim"
    label = "claim"
    verbose_name = "OpenIMIS Claims (Mock)"
