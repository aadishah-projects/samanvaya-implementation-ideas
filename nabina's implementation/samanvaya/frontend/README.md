# Samanvaya Frontend

Vite + React + Tailwind CSS dashboard for the Samanvaya reconciliation backend.

## Run

```bash
npm install
npm run dev
```

The dev server proxies `/api` requests to `http://127.0.0.1:8000`.
